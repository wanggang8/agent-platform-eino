---
scope: Agent Workbench 全平台及 FOBrain 实验能力
purpose: 为后续 spec、epic、story 与实现提供一致性契约
altitude: initiative
updated: 2026-07-15T12:11
---

- (direction) 采用教练式路径；仅交付 build-substrate Architecture Spine，不额外制作团队说明文档。
- (constraint) 平台级 initiative altitude；后续 feature、epic、story 必须引用本 spine 的稳定 AD 编号。
- (constraint) 继承 Eino-first、Product Facts 与 StructuredResult 单一事实材料、Workbench/Action API 同源投影、写域 policy/approval/audit、旧项目只读参考且禁止复制旧 runtime/接口/DOM。
- (constraint) 继承已定 UX：桌面浅色三栏、聊天是查询与动作唯一入口、IA-02 只读、result_ref/ActionDraft 冻结范围、快照每页100条、写操作二次确认与逐条回读。
- (constraint) 现有仓库现实：Go 1.22、Eino v0.9.12、modernc SQLite v1.34.5；internal/einoapp 已按 httpapi/execution/facts/product/capabilities/llm/observability/providers/store 分层，前端已采用 Vite/React 契约边界。
- (constraint) 现有 ADR 已固定配置文件为后端唯一配置源、HTTP 成功业务schema直出、SQLite事实仓储与事务边界、Eino event仅作为事实输入、provider raw payload不得越界、写域在HITL前禁用。
- (question) UX 已确定仅桌面浅色且不做移动端，但 docs/02、frontend-architecture、实施与验收计划仍要求 mobile；架构定稿前必须新增 ADR 并同步这些上游规范。
- (direction) 当前阶段是架构规划而非实现；只修改 architecture planning artifact 与必要的规范/ADR，同步前不触碰业务代码。
- (constraint) 源码 reality-check：Eino 当前用于 ChatModelAgent/Runner、AgentEvent转换、InvokableTool/ToolInfo、callback 和 CheckPointStore 接口；Product Facts、run/pending状态、policy、approval幂等、HTTP/SSE、projection、audit/replay与provider边界由项目实现。
- (question) 当前显式 capability 路径由项目 ToolLoopRunner 直接调用 Eino InvokableTool，ChatModelAgent 配置 MaxIterations=1 且未承载完整工具循环；架构需决定目标是否要求所有自然语言工具编排统一进入 Eino Runner。
- (decision) 总体范式采用六边形架构：核心端口由 execution/facts/product/capabilities 定义，HTTP、Eino、LLM、SQLite、FOBrain 与前端是适配器；绑定全平台，防止 provider、存储和传输逻辑反向渗入核心。
- (decision) Runtime 采用 Eino-first 而非 Eino-only：自然语言 Agent、模型工具选择、Tool Loop、Runner Event、interrupt/checkpoint/callback 归 Eino 执行面；Product Facts、policy、approval、幂等事务、安全投影、HTTP/SSE、audit/replay 归项目控制面。
- (decision) 明确 Action API 可绕过模型执行确定性命令，但不得绕过 capability registry、policy、approval、StructuredResult Safety Gate 与 Product Facts；自然语言工具编排必须统一进入 Eino Runner。
- (evidence) 2026-07-14 核验 CloudWeGo 官方文档：ChatModelAgent 在配置 Tools 后原生执行 ReAct 工具循环，并提供工具调用 middleware；HITL、interrupt/checkpoint 与 callback 均是 Eino 执行面的正式扩展点，因此推荐的“Eino 执行面 + 项目控制面”可基于框架公开能力实现，而非自建第二套 Agent runtime。来源：https://www.cloudwego.io/docs/eino/core_modules/eino_adk/agent_implementation/chat_model/ 、https://www.cloudwego.io/docs/eino/core_modules/eino_adk/agent_hitl/ 、https://www.cloudwego.io/docs/eino/core_modules/chain_and_graph_orchestration/checkpoint_interrupt/ 、https://www.cloudwego.io/docs/eino/core_modules/chain_and_graph_orchestration/callback_manual/
- (decision) Eino 版本线保持当前稳定版 v0.9.12；不采用已有 v0.10.0 alpha 预发行线。升级必须单独做兼容性验证并形成 ADR，不能在业务 story 中顺带升级。
- (decision) Product Facts 持久化采用“当前状态/聚合快照 + 单调递增的不可变事实与审计记录”，不采用完整 Event Sourcing/CQRS；产品读取直接访问当前状态，历史记录用于来源追踪、审计、诊断与受限 replay，不承诺通过全量事件回放重建整个数据库。
- (constraint) QueryResultSnapshot 与 ActionDraft 是一等 Product Facts：前者冻结一次查询的有序对象集合、分页边界、来源与版本，后者冻结待执行对象、动作参数、接收人、依据快照和摘要哈希；LLM 对自然语言的后续理解不能替代这些持久化引用。
- (decision) 所有写域采用不可变 ActionDraft：解析自然语言与 result_ref 后创建草案，展示结构化确认摘要；确认时校验 actor、draft digest、权限、policy 与事实有效性；执行后逐条回读并追加 Product Facts。任何目标、参数或接收人变化都必须创建新草案，禁止就地修改已展示或已确认草案。
- (constraint) 用户确认绑定 action_draft_id、draft_version、digest 与 actor；确认后不得重新查询并静默扩大、缩小或替换执行对象。事实失效或权限变化时草案进入 expired/rejected，并返回可解释原因。
- (decision) 批量写操作采用逐条执行、逐条回读与显式部分成功语义；每个对象形成独立 item result，成功项不得因其他项失败而被重新执行，失败项可从原草案派生新的重试草案并保留来源链。
- (constraint) 不假设 FOBrain 支持跨对象原子事务，不实施自动补偿回滚；幂等键至少绑定 action_draft_id、item identity、action type 与 attempt lineage，重复确认不能重复写入已确认成功的对象。
- (decision) 首版采用模块化单体：一个 Go 后端部署单元承载 httpapi、execution、facts、product、capabilities、llm、observability、providers 与 store/sqlite；React Workbench 独立构建并作为静态资产交付，不引入微服务或混合前后端框架。
- (constraint) 模块化单体只合并部署，不取消端口边界；跨层只能通过已定义接口、命令、事实对象和注册表连接。SQLite 是首版事实存储适配器，核心层不得依赖 SQLite 类型，以保留未来迁移数据库或拆分服务的能力。
- (decision) Run 采用持久化、连接无关的生命周期：HTTP 创建或恢复 Run，execution 在后台推进，SSE 仅订阅由 Product Facts 投影出的增量；页面刷新、SSE 断线或浏览器关闭不得改变 Run 的事实状态。
- (constraint) queued/running/waiting_approval/resuming/terminal 等状态必须持久化；waiting_approval 可跨进程重启恢复。HTTP handler 与 SSE writer 不得直接持有唯一执行状态，也不得把客户端断线解释为业务取消。
- (decision) 进程恢复采用持久化 Run 状态与有期限执行租约：可证明幂等的只读步骤可恢复；外部写请求在响应丢失或状态不明时进入 reconciling，必须按已冻结对象逐条回读，禁止启动后盲目重试。
- (constraint) 恢复分类至少区分 safe_to_resume、waiting_approval、reconciling_write、manual_attention 与 terminal；无法通过回读证明写入结果时进入 manual_attention，并保留 ActionDraft、attempt 与安全错误证据，不得把未知伪装为失败或成功。
- (evidence) 现有 FOBrain 接入文档只支持 workspace 共享 token，并由 current_user_context 读取该凭据对应的当前用户；当前没有应用自身登录、SSO 或每用户凭据绑定，因此“操作人身份”仍是写域实施前必须显式收口的边界。
- (decision) 本轮实验采用受控单操作人模式：actor 只能来自 workspace FOBrain 凭据实时解析出的 current_user_context，并与 ActionDraft、approval、attempt 和 audit 绑定；无法解析或权限上下文不一致时写域 fail closed。
- (constraint) HTTP/前端/LLM 不得提交或覆盖 actor。共享凭据下无法区分多名实际操作者，因此首版不得声明多人审计能力；开放多人使用前必须新增产品认证、会话身份、每用户或委托式 provider 凭据绑定，并重新验证权限与审计链。
- (decision) 首版 SQLite 绑定单后端实例部署，启用 WAL、外键、busy timeout、受控事务与可验证备份；不支持多个进程或多台主机共享同一 SQLite 文件。
- (constraint) 多实例、高可用、多人并发写或共享网络文件系统需求触发存储迁移 ADR，默认候选为 PostgreSQL；迁移前核心只依赖 facts repository/checkpoint ports，禁止泄漏 SQLite SQL、事务对象或 row type。
- (decision) result_ref 解析采用“模型提取指代意图，程序确定性消歧并锁定”：显式 ref、序号、时间或查询标签必须唯一匹配；仅有一个合格候选时可自动采用；存在多个合理候选时必须返回 clarification_required，禁止模型猜测或默认最近一次。
- (constraint) ActionDraft 只能引用 repository 中存在、归属当前 workspace/conversation/actor、未过期且对象集合可验证的 QueryResultSnapshot；客户端传入的 result_ref 与模型生成的 ref 均视为不可信输入。
- (decision) 上下文压缩只压缩自然语言历史：持久化摘要包含安全语义、未决问题及允许继续引用的 result_ref/action_draft_id，不复制 raw provider payload 或整批行数据；需要事实时由 Context Assembler 通过 facts repository 重新加载安全投影。
- (constraint) 摘要、模型记忆和聊天文本均不是事实来源；摘要中的引用必须重新执行归属、有效期与权限校验。上下文窗口不足只能导致进一步压缩或澄清，不能降级为根据摘要重建操作对象。
- (decision) QueryResultSnapshot 必须声明 coverage=complete_set|partial_page、provider pagination cursor/边界的安全表示、total_count 可知性和 ordered item refs；“全部”动作只允许引用 complete_set，Workbench 分页仅是同一快照的产品投影。
- (constraint) 完整集合构建期间任一页失败时不得发布 complete_set；只能形成带缺口的 partial/incomplete 事实并禁止全量 ActionDraft。确认后不得重新查询来替换已经冻结的集合。
- (decision) QueryResultSnapshot 与 ActionDraft 的有效期由 capability metadata/policy 配置，不在业务分支中统一硬编码；事实必须保存 captured_at、expires_at、source version/etag（来源支持时）和用于动作校验的关键字段版本。
- (constraint) 过期、来源版本冲突或关键事实变化时必须拒绝旧草案，并通过刷新查询创建新 snapshot 与新 draft；不得在确认阶段静默替换对象、负责人、状态或其他确认内容。
- (decision) Workbench 聊天与 Action API 共用 PrepareAction → ConfirmAction 二阶段写协议和同一 application command；聊天路径由 Eino interrupt/resume 暂停与恢复，Action API 返回 approval_required 及安全草案引用，不存在绕过确认的一步写入口。
- (constraint) 是否属于写域、审批策略与允许动作由 capability metadata + policy 决定，LLM、HTTP 路由和前端不得自行决定；所有修改类能力首版默认 require_confirmation，改变默认值必须形成安全 ADR。
- (decision) 外部写操作成功必须同时满足 provider 明确接受写入和该 capability 定义的逐条回读验证；仅 HTTP 2xx、provider success 字段或模型描述均不足以形成 succeeded 事实。写入成功但回读不一致进入 verification_failed/reconciling，不伪报完成。
- (resolution) 已新增 ADR `docs/adr/2026-07-14-desktop-only-first-release.md`，并同步 UX、前端架构、实施计划、验收计划、视觉矩阵、工具规范和视觉 fixture；既有 mobile 实现/截图保留为非约束历史证据，首版门禁只要求 desktop 1440×900。
- (evidence) 前端版本核对时，`npm --prefix web/eino-workbench ls` 因 npm workspace 依赖提升误报子目录依赖缺失；改用根 workspace 命令 `npm ls --workspace=@agent-platform-eino/eino-workbench --depth=0` 后确认 lockfile 安装版本。架构 Stack 使用 workspace 解析结果，不使用失败命令的误判。
- (review) Reviewer gate 初审 verdict：rubric=CHANGES_REQUIRED，reality=REJECT，seams=NOT_IMPLEMENTATION_SAFE；核心问题为写域 readiness gate 缺失、目标态被误写成当前现实、v2 数据/状态/API 接缝不完整、真实 Eino ReAct/HITL 尚未迁移、SSE sequence 与 SQLite 连接策略未落地。
- (resolution) 新增 `docs/adr/2026-07-14-product-facts-v2-eino-runtime-migration.md`，固定 v1 历史只读、v2 聚合与安全 StructuredResult 持久化、事实序列、状态映射、真实 Eino chat HITL、确定性 Action API continuation、checkpoint/attempt 事务协议、verifier SPI、SQLite/健康/清理及 Go/Node 工具链迁移。
- (resolution) Spine 增加 Current Implementation Gates，并用 `[ADOPTED TARGET]` 区分已确认目标与当前实现；真实写域继续被 G-FACT/G-WRITE/G-SAFE 阻塞，当前只允许只读事实链与 v2 契约/迁移准备。
- (resolution) Eino seam 修正为受控 kernel dependency：仅 execution/capabilities/observability/store/sqlite 可直接 import Eino；facts/product/httpapi/providers/web 保持 Eino-free，execution 是 Run/Action 状态迁移唯一命令所有者。
- (evidence) 2026-07-14 官方核验：Go 支持策略当前支持 1.25/1.26，Node 官方建议生产只用 LTS 且 v24 为 LTS，SQLite WAL 需显式启用并检查返回值、busy timeout 是连接级设置；draft canonical digest 采用 RFC 8785 JCS + SHA-256。
- (resolution) 将 G-ARCH-V2 与 G-TOOLCHAIN 同步进入唯一权威 `implementation-readiness-gate.md`；AD-16 与 Deferred 明确持久化 SSE/FactEvent 是 target 而非当前现实；ContinuationRef 明确区分聊天 Eino checkpoint 与确定性 Action API project continuation。
- (review) Final reviewer gate：rubric=PASS，version/reality=PASS，incompatible-units=IMPLEMENTATION_SAFE；剩余事项均为已登记实施门禁，不再是架构歧义。
- (direction) Architecture Spine 状态置为 final；下一层必须先为 Product Facts/Catalog/Action/Resume/SSE v2 生成机器契约与实施切片，G-ARCH-V2/G-TOOLCHAIN/G-FACT/G-WRITE/G-SAFE 未通过前禁止真实外部 mutation。
- (decision) 2026-07-15：核验结果采用唯一时间边界映射；verification policy 预算内不一致为 reconciling/待核验，明确失败或预算结束仍未知为 failed 或 manual_attention/待排查，任何未知结果都禁止重复 mutation。
- (decision) 2026-07-15：操作记录是 Product Facts/ActionResult 的 actor-scoped 安全只读投影；固定入口复用 IA-01 历史模式，详情进入 IA-02，不从聊天摘要或 FOBrain 重建，不产生重试能力。
- (constraint) 2026-07-15：操作记录默认发现最近六个月；未关闭的 waiting/running/reconciling/manual_attention 引用不受默认窗口清理，物理保留继续服从事实与审计策略。
- (decision) 2026-07-15：settle window 在 attempt 首次原子转为 sent、且外部 mutation 调用前计算并持久化 sent_at/settle_deadline；verification call 先预留并扣减预算，崩溃不返还，重启不得重置。
- (decision) 2026-07-15：deadline/预算耗尽后的未知或 mismatch 一律 manual_attention；只有 provider 明确拒绝且证据能证明 mutation 未发生才 definitive failed，消除 Run/ActionResult 双终态。
- (decision) 2026-07-15：首版没有待排查关闭命令；failed/manual_attention 产生 attention_status=open，并在未来版本化 closure fact 出现前持续可发现。六个月窗口使用服务端 UTC+8 日历月 cutoff、包含边界、operation_at 倒序与稳定后端 union 分页。
- (decision) 2026-07-15：历史读取要求 actor/workspace 精确相等并按 frozen history_access_policy_ref 证明当前对象权限；未知/拒绝时整批 fail closed，不过滤后重算。provider instance 变化不做身份别名，首版 actor 文案为凭据主体。
- (decision) 2026-07-15：写域新增 item-level retry lineage 与跨 draft semantic mutation guard；reserved_not_sent 才可首次发写，sent 之后永远只核验；API 区分 read_retryable、verification_resumable 与 mutation_retry_allowed。
- (decision) 2026-07-15：新增 AD-27 固定 opaque 产品 entity_ref 与 provider locator 分离；locator 只在 facts/provider 受控边界保存和解析，产品/LLM/前端不得拆 ref 获取真实 provider ID。
- (constraint) 2026-07-15：F-07 七项代表性只读能力逐项绑定已批准 tool id；FR-19/20 字段证据不完整与产品 v2 投影受 G-READ-03 阻塞，business_list 不得替换为 my_business_systems。
- (change) 2026-07-15：run lifecycle 与迁移 ADR 已同步 sent 原子边界、持久 deadline/预算、唯一 verifier outcome、跨草案 semantic mutation guard、open attention 历史并集和 opaque ref/locator 分离；待重新运行 reviewer gate。
- (change) 2026-07-15：正式 backlog 已按 M-0～M-5 重切为 Story 1.1～1.37，Story 1.36 承接 AD-26；Epic 2/3 只保留各 5 个证据/Mock Story，真实动作保持 TC 非 Sprint。
- (fix) 2026-07-15 reviewer rerun：真实写域放行条件补齐共同 G-ARCH-V2 与 G-TOOLCHAIN，和唯一 readiness gate／Candidate conversion gates 保持一致。
- (resolution) 2026-07-15 targeted seam closure：semantic mutation 使用 `smk.v1` + RFC 8785 JCS 规范化 payload，并由 Confirm 事务取得唯一 SemanticMutationClaim；任何既有 claim 阻断普通草案，仅 definitive failed 的逐条人工 retry 可事务转移 claim。
- (resolution) 2026-07-15 targeted seam closure：ActionAttempt 改为 phase 判别 union，`reserved_not_sent` 禁止 sent-only 字段；VerificationEvidence 固定可编译判别字段并由唯一 reducer 产生 outcome，manual_attention 聚合只允许 partial/none_succeeded。
- (resolution) 2026-07-15 targeted seam closure：operation_at 在首次发布固定，服务端生成 as_of 与 Product Facts 高水位，Asia/Shanghai 六日历月算法、opaque 防篡改 cursor、open attention 并集和稳定错误均成为后端唯一协议。
- (resolution) 2026-07-15 targeted seam closure：内部 entity_subject_key 唯一连接随机 entity_ref、跨查询身份、semantic guard 与独立 ProviderLocator；LocatorRepository/Resolver、history policy 传播、损坏语义和引用保留均已固定。
- (resolution) 2026-07-15 final seam closure：VerificationEvidence 改为 readback/non-application/control-loss 判别 union，唯一 reducer 先处理 control certainty，再消费 provider acceptance；rejected/unknown+match 不得成功，非 readback 分支不伪造 observed。
- (resolution) 2026-07-15 final seam closure：跨 Run 操作历史不再复用 Run-local SSE sequence；首个请求事务性物化 immutable OperationListSnapshot，cursor 绑定独立 opaque ref，跨 Run 变化不进入后续页。
- (resolution) 2026-07-15 final seam closure：entity subject 改为随机不可变 ID，版本化 HMAC 仅作 active/retained fingerprint alias；secret/locator key 轮换不得改变 subject、claim 或 locator lineage。
- (resolution, superseded in part) 2026-07-15 rubric closure：Confirm 后 sent 前 cancel/stop 与发送使用 draft 级全有或全无 phase CAS；取消获胜写 cancelled_before_send、零 mutation，sent 获胜整体 action_already_sent 并继续核验。统一释放 claim 随后被来源感知 disposition 取代。
- (resolution) 2026-07-15 incompatible-unit closure：claim 取消语义按来源分流——首次 claim 写 released_zero_write，failed-retry claim 恢复 predecessor failed；audit tombstone 不充当权威 claim。AC-33 统一为 OperationListSnapshot/operation_list_snapshot_ref。
- (gate) 2026-07-15 final reviewer gate：Reality PASS；Good-spine PASS（High=0、Medium=0）；Incompatible Units IMPLEMENTATION_SAFE。规划架构可进入 Sprint Planning，真实写域门禁不变。
