---
topic: FOBrain 漏洞处置实验 Agent 桌面端 UX
goal: 形成 DESIGN.md 与 EXPERIENCE.md 两份可实施 UX 契约
updated: 2026-07-15T12:07
---

- (decision by user) 仅设计桌面端 Web Workbench；不做移动端适配、移动布局或触屏专用交互。
- (decision) 以已确认的9份产品蓝图、Product Issues、实施门禁和2026-07-14目标部署只读证据作为本轮UX输入。
- (constraint) 只读事实链可进入设计；四类写动作只能设计确认、阻断、回读和失败状态，不得假设真实写接口可用。
- (override by user) 用户最新决定覆盖历史UX范围：本轮及产品目标只做桌面端，不做移动端适配。现有 docs/02-ux-visual-requirements.md、docs/frontend-architecture.md、docs/07-implementation-plan.md、docs/08-acceptance-plan.md 仍要求 mobile；进入架构/实现前必须形成ADR并同步这些规范。
- (constraint) 桌面端既有技术边界继续适用：Vite+React+TypeScript、React Router、TanStack Query、Zustand、Radix UI Primitives、lucide-react、项目CSS；左导航+中间时间线+右Inspector，前端只消费后端Product Facts投影。
- (constraint) 历史视觉规范中继续有效的桌面约束：第一屏即工作台；左导航、中聊天时间线、右Inspector；专业、密集、聊天优先；卡片层级克制；中文产品文案；不展示raw JSON、provider字段、内部ID、token、密钥或英文内部状态码。
- (constraint) 产品不新增消息、邮件、webhook、催办或通知中心；FOBrain自身通知既不在本产品界面中配置，也不作为成功判定。
- (question) 待决定：桌面端暗色模式、动效、离线行为和是否只提供中文；这些不是当前只读流程的范围门禁。
- (event) 9份确认来源的UX事实提取完成，产物为 .working/source-extract.md；业务旅程、事实字段、安全边界已闭合，信息架构仍需决定聊天与百条级操作队列的衔接。
- (decision by user) Agent聊天完成查询后只显示新增漏洞数量与安全摘要；用户点击摘要进入专门的漏洞操作工作区。百条级漏洞不得全部展开在聊天时间线，也不在聊天内嵌完整操作列表。
- (decision by user) 专门操作工作区保留左侧会话导航；中间为高密度漏洞队列；右侧改为动态详情与操作面板。右侧默认“处理”，并提供“事实”“执行记录”“技术信息”次级标签；技术信息仅管理员或排障使用。
- (decision) 聊天模式右侧可承担通用证据/运行Inspector；进入漏洞操作工作区后，同一右栏优先承载当前选中漏洞的业务操作，审计与技术诊断不抢占默认视图。
- (override by user) 纠正交互主模型：查询、派发、转发、误报、延时都由用户在聊天中表达意图，AI直接选择并调用相应工具；不能把主要操作改造成用户进入专门页面手工完成。
- (decision) 百条级专门工作区降为从聊天摘要进入的明细核对表面，用于查看完整事实、范围和逐条结果；动作准备、审批、执行和结果反馈仍回到聊天时间线。
- (constraint) 所有写工具继续遵守产品级二次确认：用户的自然语言写意图只生成待确认操作，不立即写入；聊天审批卡逐条展示影响对象与目标变化，执行人确认后才能调用写工具，随后逐条回读。
- (decision by user) 多轮中的“这些漏洞”等指代由AI按上下文理解，但任何外部写入范围必须由程序通过安全结果引用锁定；程序不按自然语言关键词硬编码业务分支。
- (constraint) 上下文压缩不得丢失或重建业务事实：查询结果集合、动作草案、审批和回读引用必须持久化在Product Facts中；AI上下文只接收安全摘要和引用，引用缺失、失效或歧义时重新查询或澄清。
- (event) 已生成 .working/result-reference-architecture-handoff.md，记录多轮结果快照、动作草案、审批绑定、上下文恢复、歧义和测试要求；UX定稿后交给bmad-architecture正式固化。
- (decision by user) 业务系统负责人或运维负责人缺失时仍允许派发；缺失字段必须明确展示，操作人继续从FOBrain全部人员列表手动指定修复负责人。AI不得推断、补全或自动推荐。
- (constraint) 业务/运维负责人缺失不进入“数据不足阻断”状态；它只降低人工判断信息完整性。人员列表读取失败、目标人员无法唯一确定、无权限或结果引用无效仍必须阻断写操作准备。
- (decision by user) 缺失字段使用确定性文案：当前修复负责人缺失显示“未分配”；业务系统、业务系统负责人或运维负责人缺失显示“未提供”；相关界面不以空白表达缺失。
- (decision by user) 百条级写入确认卡默认展示动作、目标负责人、总数和前5条漏洞；“查看全部”进入专门明细工作区查看所有对象；确认/取消保留在聊天卡中；不强制逐条点开，但确认前全部对象必须可查看。
- (decision by user) 不沿用当前Workbench视觉基线；重新设计视觉语言，通过多个桌面工作台方向对比后选择。专业、密集、聊天优先及既定安全交互保持不变。
- (event) 已生成并静态验证3个全新桌面Workbench视觉方向：precision-command、editorial-review、signal-matrix；英雄场景和交互完全一致，仅视觉人格、密度与层级不同。应用内自动预览连接失败，方案HTML本身结构与安全检查通过。
- (decision by user) 选择视觉方向1 precision-command（精密指挥台）作为DESIGN基线：冷静、专业、高密度、边界清晰。方案2和方案3不采用。
- (decision by user) 首版仅提供浅色主题；不设计暗色模式、主题切换或暗色token。
- (event) 完整 Reviewer Gate 已运行：rubric、可访问性与安全、边界与恢复三路评审均无 critical；统一报告已生成，确定性问题开始回写 spine。
- (change) 根据评审补齐当前 READ-01 只读旅程、人员选择卡门禁、审批过期与待核验等恢复状态、焦点与播报协议、安全执行记录、多来源/子集引用和 UJ-05 异常流程；批量确认口径同步为前5条加查看全部。
- (decision) OQ-01 已决策：百条级漏洞明细采用冻结快照分页，每页100条；固定总数与顺序，保留页码、当前行和滚动位置，使用单一 grid 焦点的键盘行导航。
- (decision) OQ-03 已决策：高密度表格单行摘要，POC视觉截断，多个IP显示首个加其余数量；右栏展示完整POC与全部IP；用户可见时间固定为 YYYY-MM-DD HH:mm:ss（UTC+8）。
- (decision) 正式关键屏范围选定为三张：当前只读聊天、冻结快照分页漏洞明细、门禁后的目标写入审批与部分失败；人员选择等后续门禁组件保持 spine-only。
- (event) 开放项已分期：OQ-02 与 OQ-04 分别阻断未来人员写域和直接延时写域；OQ-05 由架构定义保留期；OQ-06～OQ-08 属后续恢复、快捷键和技术信息门禁，不阻断当前只读 UX 定稿。
- (event) spines finalized
- (decision) 2026-07-15：OQ-05 关闭为 RD-03；左栏固定操作记录入口不是聊天会话，复用 IA-01 历史模式，选择记录后进入 IA-02 只读逐条详情。
- (decision) 2026-07-15：操作记录默认按时间倒序发现最近六个月；未关闭的待确认、执行中、待核验和待排查对象在关闭前持续可发现，六个月不是物理删除策略。
- (change) 2026-07-15：ST-19 与 ST-27 统一为时限分流；核验预算内不一致显示待核验，明确失败或预算结束仍不一致显示待排查，不提供再次执行。
- (override) 2026-07-15：依据 Owner 已批准 Correct Course Batch 提案，本次 UX update 跳过再次逐项询问与可选 reviewer gate；后续由全局 Implementation Readiness 复核。
- (fix) 2026-07-15 readiness 对齐：UJ-01～UJ-04 与正式目标 Story 的写入结果统一按 deadline／调用预算分流；预算内 unknown/mismatch 显示“待核验”，明确未应用或预算耗尽仍无法证明才显示“待排查”，禁止从两种文案推导再次 mutation。
