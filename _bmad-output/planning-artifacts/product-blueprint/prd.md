---
title: FOBrain 漏洞处置实验 Agent PRD
status: BASELINE_APPROVED_PENDING_IMPLEMENTATION_GATE
created: '2026-07-13'
updated: '2026-07-15'
scopeBaseline: experiment-scope-proposal.md
---

# PRD：FOBrain 漏洞处置实验 Agent

## 0. 文档目的与状态

本 PRD 将已确认的真实运营场景收束为可验收的实验产品需求，供后续架构、UX、Epic 和实现计划使用。产品需求已形成基线，但外部写域能力尚未在目标部署验证；因此本 PRD 不是实施授权。逐条 Given/When/Then 以 [Product Issue 索引](product-issues/README.md) 与各 PI 文件为准，追踪关系见 [需求追踪矩阵](requirements-traceability-matrix.md)。

## 1. 愿景与边界

在 FOBrain 已有漏洞闭环之上，提供一个让运营基于真实、授权事实完成受控人工处置的实验 Agent。它组织事实和流程，不替代业务判断，不因模型推断而改变外部数据。

本轮只验证“读—人工判断—确认—写—回读”的闭环，不建设完整漏洞管理平台，不恢复全部工具，也不交付自动派发、通知或工单闭环。

## 2. 用户与关键旅程

### 2.1 目标用户

- **产品中心／运营：** 在 FOBrain 内置权限可见范围内查看并处置漏洞。
- **管理员：** 执行直接修复延时的唯一授权角色。
- **FOBrain 系统负责人：** 对写入失败、回读不一致及异常进行排查。

### 2.2 非用户／非角色

- AI 不是接收人、误报或延时的决策主体。
- 本产品不是通知服务、自动派发器、FOBrain 权限管理器或漏洞修复执行方。

### 2.3 关键旅程

- **UJ-01：运营手动派发新增漏洞。** 运营查看按发现时间倒序的全部新增漏洞，逐条查看统一卡片及派发判断事实，选择接收人；同一接收人可合并；二次确认后逐条回读负责人变更。
- **UJ-02：运营主动转发责任人。** 运营在自己可见的漏洞中选择需转发项，从全员列表选择接收人；同一接收人可合并；确认后逐条回读。
- **UJ-03：管理员直接修复延时。** 运营与相关人员已沟通确定期限，管理员查看单条漏洞与新期限／可选原因，二次确认后以状态和期限回读核验。
- **UJ-04：运营标记误报。** 运营对自己负责的漏洞作出人工误报判断，二次确认后以“误报”状态回读核验。
- **UJ-05：处置异常。** 任一写入失败、部分失败或核验暂时无法收口时，操作者逐条看到完成、待核验或待排查状态。待核验项只沿同一执行记录继续核验，不重复写入；明确失败或核验收敛后仍不一致的项保持待排查，未完成项不被伪报成功，并由 FOBrain 系统负责人排查。

## 3. 统一业务词汇

| 词汇 | 本 PRD 中的定义 |
| --- | --- |
| 统一漏洞卡片 | POC、IP、IP 在线／离线状态、业务系统、修复负责人；是列表、确认和结果的共同事实展示。 |
| 二次确认 | 执行人确认将要执行的对象集合和变更后，产品才允许外部写入。单条直接展示完整对象；多条时确认卡默认展示前 5 条，并在确认前提供“查看全部”进入只读完整冻结集合。 |
| 回读核验 | 写入后重新取得外部事实，按动作目标判断成功；不能只使用接口返回成功。 |
| 待核验 | 写请求可能已经到达 FOBrain，但在既定核验期限或调用预算内暂时无法确认最终结果的过渡状态；只能沿同一执行记录继续回读，不能重复写入。 |
| 待排查 | 写入明确失败，或核验期限／预算结束后仍无法证明目标达成的未完成终态；由 FOBrain 系统负责人排查。 |
| 内置权限范围 | FOBrain 接口返回给当前用户的可见对象范围；产品不得扩大或自行推断。 |

## 4. 功能需求

### F-01：授权事实与统一卡片

- **FR-01：** 产品必须只使用当前用户已授权的 FOBrain 事实展示漏洞及关联上下文。
- **FR-02：** 产品在漏洞列表、二次确认和结果展示中必须呈现统一漏洞事实。单条直接展示完整对象；多条时确认卡默认展示前 5 条并提供“查看全部”，使执行人在确认前可以核对完整冻结集合；不得仅用数量替代待写对象明细。
- **FR-03：** 产品必须区分真实空结果、无权限、数据不足和外部失败；不得由 AI 虚构“没有待处理漏洞”等结果。

### F-02：每日新增漏洞手动派发

- **FR-04：** 运营必须能够查看全部新增漏洞，按发现时间倒序；无结果时展示“没有待派发漏洞”。
- **FR-05：** 运营必须先查看漏洞、IP 在线状态、业务系统、业务系统负责人和运维负责人，再从 FOBrain 全部人员列表选择接收人；AI 不得替代该决定。
- **FR-06：** 仅已逐条判断且最终选择同一接收人的漏洞可合并派发；不同接收人不得混合。
- **FR-07：** 派发仅在二次确认后写入；每条漏洞只有接口成功且回读修复负责人为指定人员时才完成。

### F-03：责任人转发

- **FR-08：** 当前用户可主动对其内置权限范围内的漏洞发起转发；不要求系统识别错派或先发生其他动作。
- **FR-09：** 转发接收人必须从 FOBrain 全部人员列表选择；同一接收人可合并、不同接收人必须拆分。
- **FR-10：** 转发仅在二次确认后写入；逐条回读负责人为目标人员时才完成。

### F-04：直接修复延时

- **FR-11：** 运营与相关人员人工沟通后确定新修复期限；产品不得自动决定是否延时或期限。
- **FR-12：** 直接延时一次只处理一条漏洞，延时原因可不填；只有管理员完成二次确认后可执行。
- **FR-13：** 只有回读状态为“延时”且修复期限等于指定新期限时，结果才显示“延时成功”。

### F-05：误报标记

- **FR-14：** 操作人可对自己确认负责的漏洞发起误报标记；误报判断必须由人作出，不能扩展为自动规则。
- **FR-15：** 误报标记仅在二次确认后写入；只有回读状态为“误报”时才显示标记成功。

### F-06：共同安全与结果要求

- **FR-16：** 任一写动作在未完成二次确认或取消确认时，不得向外部系统写入。
- **FR-17：** 任一写动作必须逐条区分完成、待核验和待排查。当前无法确认且仍可沿同一执行事实链继续核验时显示“待核验”；明确失败，或核验期限／调用预算结束后仍不一致、未知时显示“待排查”。成功项可独立完成；任何未完成项不得伪报成功，也不得通过重复外部写入完成核验。
- **FR-18：** 本产品不得发送消息、邮件、webhook、催办或通知中心通知；FOBrain 自身通知不构成本产品功能或成功条件。

### F-07：代表性只读能力

- **FR-19：** 产品必须能够读取当前 FOBrain 用户接口提供的安全身份、部门和角色上下文，用于解释凭据主体和权限范围；部门等源字段未提供时必须显示稳定的“源字段不可用／未提供”，不得推断或伪报已解析，也不得展示 token、原始账号对象或凭据。
- **FR-20：** 产品必须能够读取并安全展示当前用户接口提供的 FOBrain 权限和数据范围，并明确区分 `resolved`、`confirmed_empty/no_permission` 与 `source_field_unavailable/unknown`；未知不得折叠为无权限或通过，不得展示原始 policy payload。
- **FR-21：** 产品必须能够按 IP 查询资产列表及其安全在线状态，作为统一漏洞事实和资产核对上下文。
- **FR-22：** 产品必须能够从资产列表进入单个资产的安全详情；安全引用不得冒充 provider 的真实资产 ID。
- **FR-23：** 产品必须能够按 IP 查询关联漏洞列表，展示安全漏洞事实并支持资产—漏洞联查。
- **FR-24：** 产品必须能够从漏洞列表进入单个漏洞的安全详情，为人工处置前判断提供事实。
- **FR-25：** 产品必须能够通过本轮已批准的 `business_list` 能力查询 FOBrain 全部业务系统安全列表，为统一漏洞卡片和业务上下文提供事实；可见范围只由 FOBrain 接口内置权限裁剪，不得替换为“我的业务系统”等已排除能力，也不得展示原始 provider payload。

## 5. 跨功能边界

- **NFR-01（安全事实）：** 外部原始返回不能直接作为产品展示、审计或模型上下文事实；产品只消费安全、结构化的业务事实。
- **NFR-02（权限边界）：** 产品不得扩大 FOBrain 权限、代替 FOBrain 管理权限，或将查询不到的对象视为可操作对象。
- **NFR-03（写入完整性）：** 所有数据修改均须绑定执行人二次确认、客观回读和逐条失败可见性；未确认、取消、拒绝或过期时外部写入数必须为零。
- **NFR-04（证据边界）：** 不把目标部署 API 已验证等同于产品已集成；不得对写入结果、写域状态值域或权限作静默假设。

## 6. 本轮范围与非目标

**范围内：** 7 个代表性只读能力、四项受控写动作、统一卡片、人工判断、二次确认、逐条回读、失败呈现。

**范围外：** 自动派发及其配置、自动决定接收人／误报／延时、其余 FOBrain 工具恢复、通知、完整漏洞管理、工单与修复闭环、公司主机在线盘点的独立交付、生产 SLA 和商业化定位。

## 7. 验收与实验判定

| 判定 | 客观证据 |
| --- | --- |
| 受控写入 | 取消或未确认时没有外部写入记录。 |
| 成功可信 | 每个成功项具有动作目标的回读事实。 |
| 失败可见 | 每个暂时未知项逐条保留待核验；每个明确失败或核验收敛后仍未知／不一致项逐条保留待排查。 |
| 人工决策保留 | 接收人、误报、延时及新期限均由操作人决定。 |
| 范围遵守 | 没有自动派发、通知或未纳入能力进入本轮。 |

## 8. 实施前阻塞项

1. 在当前产品中实现并 smoke 验证目标部署已通过的“全部新增漏洞”精确查询、倒序和空结果语义。
2. 在当前产品中实现并 smoke 验证 FOBrain 全部人员列表及接口内置可见范围。
3. 为派发、转发、延时、误报分别验证动作权限、写入、回读、部分失败及错误脱敏。
4. 验证统一卡片所需行级事实可以安全提供，并对真实缺失字段稳定显示为空／未分配。
5. 明确直接延时的新期限约束与目标部署可延时状态集合。

前两项和第 4 项可以作为只读事实链先行实现；全部写域阻塞项解决前，不得进入外部写入实现。完整准入结论见 [实现就绪门禁](implementation-readiness-gate.md)。
