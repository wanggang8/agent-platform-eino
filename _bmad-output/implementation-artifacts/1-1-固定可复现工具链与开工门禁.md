# Story 1.1：固定可复现工具链与开工门禁

Status: done

WorkItemType: enabler

Milestone: M0

## Story

As a 交付负责人，
I want 使用唯一受支持的 Go、Node 与 CI 工具链产生验收证据，
So that 后续所有实现和裁决来自一致、可复现的环境。

## Requirements

NFR-01；AR-01、AR-29、AR-30；G-TOOLCHAIN；AD-18、AD-19。

## 完成裁决

- Story 状态：`done`。
- `G-TOOLCHAIN=PASS`。
- 权威证据：[`docs/acceptance-records/story-1-1-g-toolchain-2026-07-15.md`](../../docs/acceptance-records/story-1-1-g-toolchain-2026-07-15.md)。
- 当前 canonical 结果：GitHub Actions run `29432627424` 对 clean commit `d174f79e5341dcce636d190aa0e510e4528a955a` 的 build / verify 均成功，GHCR digest、完整 baseline、desktop evidence、contract smoke、clean gates 与 30 天 artifact 同时满足 PASS 算法。
- 历史 GitLab / MCR / local preflight 失败只保留在权威记录中，不再是当前门禁状态或执行方案。

## Scope

- Go module language `1.26.0`，Go toolchain `1.26.5`。
- Node.js `24.18.0`、npm `11.16.0`。
- canonical linux/amd64 image、GitHub Actions、GHCR 不可变 digest。
- 版本校验、负向漂移测试、完整 baseline 和权威验收记录。

## Non-goals

- 不实现任何产品能力。
- 不证明 M1～M6 已完成。
- 不允许其他工具链或本地 preflight 替代 canonical 最终证据。

## Acceptance Criteria

**Given** 新环境、本机或 CI 开始构建  
**When** 执行工具链基线校验  
**Then** 只有固定 Go、Node 与 canonical image 的完整结果可发布为 G-TOOLCHAIN PASS 证据  
**And** 其他版本明确失败或标记为非通过证据。

**Given** Story 1.1 已有权威验收记录  
**When** 当前计划、门禁和 Sprint 状态读取该结果  
**Then** Story 1.1 标记 `done`，G-TOOLCHAIN 标记 `PASS`  
**And** 历史失败不覆盖当前裁决。

## Acceptance Commands

```bash
bash scripts/verify_toolchain.sh
bash scripts/run_toolchain_baseline.sh
```

实际远程证据和各子命令结果以权威验收记录为准。本次文档重切片不重新声称执行这些历史 canonical 命令。

## Exit Gate

`PASS`。允许进入 M1 的就绪复核；只有 Implementation Readiness=`READY` 后才可创建或开发 Story 1.2。
